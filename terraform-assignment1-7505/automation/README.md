# automation
automation first assignment
# terraform validate
# terraform plan
# terraform apply --auto-approve
# terraform state list | nl
# terraform output

# Testing the load balancer, and connecting to the VMs

Step 1 — SSH into each Linux VM
Use the public IPs from your output:

ssh -i ~/.ssh/id_rsa azureuser@20.220.188.167
